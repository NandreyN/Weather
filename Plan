<div class="block">
            <div class="panel panel-default" style="width:65%; height:50%; margin-left:17.5%; margin-top:5%;">
                <div class="panel-body">
                    <div class="media">
                        <div class="media-left">
                            <a href="#">
                                <img class="media-object" src="Weather_Pictures/01d.png" alt="" />
                            </a>
                        </div>
                        <div class="media-body">
                            <h4 class="media-heading">30.09.2015</h4>
                            <p>+10 G; humidity: 60%</p>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        
        JSAddingBlockLogicStruct:
          cicle:
          {
             (1) div class="media-left".Create -> appendChild(a ("href = #").append(image with id created using iterator));
             (2) div class="media-body". Create -> appendChild(<h4 class = "media-heading"/>, <p> as info);
             (3) div class="media" append(1,2);
             (4) div class="panel-body" append(3);
             (5) div class="panel panel-default" append(4) with style from html;
             div class=block append(5);
          }
          
          Plan:
            Forecast goes as 5-day forecast with data over 3 hour.
            Write a function wich will devide a forecast(hours) into forecast-data-list(days):
                Algorithm:
                  Get time property of the first list item. Transform into time objects. Main DAY data at 13.00;
                  Foreach item in itemList :
                    Compare time with time_Object_Pattern, if suits:
                      Create object, increase iterator/ Repeat
            Main func: CreateDataModel(object) GlobalFunc // forecast list as parameter  
            Inner functions: ParseDateTimeToObject, ParseDateTimeToStringInFormat, MakeItem()// div blocks
            
              
